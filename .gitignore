__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.mypy_cache/

# databases produced by demos and tests
*.chendb
*.chendb-wal
workspace/

# visualizer
node_modules/
dist/
.vite/
*.local

# editors / OS
.DS_Store
.idea/
.vscode/
.claude/

# Local working notes, not part of the published project: handover.json is the
# session-to-session context file, and it names local paths and tooling rather
# than anything about the engine. Kept on disk, kept out of the repo.
handover.json

# TypeScript incremental build state
*.tsbuildinfo

# Generated by `npm run bundle:engine`: the engine sources and the Pyodide
# runtime, copied where the browser can fetch them. Both are derived from
# things already in the repo, so committing them would only create a way for
# the WASM build to disagree with the engine.
visualizer/wasm-public/
